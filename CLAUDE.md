# קוראים חכמים — קורס הבנת הנקרא ממושחק (כיתות ג׳–ד׳)

> 📌 **מתחילים שיחה חדשה? קראו קודם את [PROJECT-STATUS.md](PROJECT-STATUS.md)** —
> סטטוס מלא, קישורים (ריפו + לינק חי), מלכודות שנלמדו, ומדריך צעד־צעד לבניית היחידה הבאה.

## מהות הקורס
קורס דיגיטלי ברמת מורכבות של Reading Eggs / Matific: **עולם משחק שלם** ולא סדרת לומדות.
מפת עולם עם תחנות, דמות מלווה שצוברת מטבעות, חנות שדרוגים לדמות, ספר מסע עם פריטים
שנאספים בכל יחידה. הלמידה מתרחשת בתוך העולם, בלי לצאת ממנו.

- **קהל יעד**: תלמידי כיתות ג׳–ד׳ עם קושי בהבנת הנקרא וכתיב
- **היקף**: 9 יחידות (0–8), 20–30 דקות ליחידה
- **מיומנויות**: הבנת הנקרא (מפורש ← הסקה), ניסוח כותרות (בחירה ← ניסוח עצמאי), משפחות מילים ושורשים (זיהוי ← כתיבה)
- **סילבוס מלא**: [syllabus.md](syllabus.md)

## עיקרון העל: מנוע קבוע, דלק מתחלף
המשחקונים **חוזרים על עצמם** בכל יחידה (בחירת כותרת, מיון משפחות, טעות בכוונה...)
עם רמת קושי עולה — והנושא של הטקסט מתחלף (משחקים, בעלי חיים, ספורט, חלל...).
הלומד יודע בדיוק מה מצפה לו; רק התוכן והאתגר מתחדשים. אין להמציא סוגי פעילות חדשים
ליחידה בודדת בלי סיבה פדגוגית מובהקת.

## מבנה קבוע לכל יחידה — חשיפה הדרגתית לטקסט
⚠️ עיקרון קריטי (פידבק מהמשתמשת): **אסור לפתוח בטקסט ארוך** — זה מרתיע. מדרגים:
1. **הטרמה** — משימה אינטראקטיבית קצרה סביב מילות הנושא
2. **משפט אחד** — מציאת מילות משפחה בתוך משפט בודד גדול
3. **משחקון ארקייד** — תופסים מילים ממשפחה בתנועה (בלונים / חלליות / דגים)
4. **קטע קצר** (2–3 משפטים) — סימון משפט מרכזי + שאלה אחת
5. **משחקון ארקייד שני** — משפחה נוספת
6. **הפסקה הגדולה** — רק עכשיו, אחרי שכל המילים מוכרות
7. **כותרות / מיון / טעות בכוונה / אנקדוטה / סיכום** — כמו בסילבוס

## בנק המשחקונים (engine.js)
| type | מה זה | תנועה? |
|------|--------|---------|
| `word-hunt` | לחיצה על מילים שמתאימות לתמונה | — |
| `sentence-hunt` | מציאת מילות משפחה במשפט גדול | — |
| `catch-game` | ארקייד: תופסים מפלצות נושאות מילים + רצף קומבו (theme: balloons / night). goal קובע אורך המשחק (ברירת מחדל ביחידה 1: 10 תפיסות) | ✅ rAF |
| `feed-game` | מפלצות בררניות: מאכילים כל מפלצת רק במילים מהמשפחה שלה (המילה עפה לפה!) | ✅ |
| `reading` | קריאה + סימון משפט מרכזי. כל הטקסט מוצג מיד (הוחלט לבטל חשיפה הדרגתית בתוך הקטע). תומך בתמונת סצנה (sceneImage + sceneSvg) | — |
| `mcq-set` | רב ברירה עם refText, פסילות והתחלה מחדש. **התשובות מתערבבות אוטומטית** — לכתוב אותן בכל סדר | — |
| `sort-families` | גרירת מילים לתיבות שורש (הוחלף ביחידה 1 ב-feed-game; זמין ליחידות אחרות) | — |
| `anecdote` / `summary` | תגלית שפה / ספר מסע | — |

## חוויה (Juice)
- **סאונד**: `sound.js` — WebAudio מסונתז בלי קבצים (pop / correct / wrong / coin / chomp / streak / fanfare). כפתור השתקה בכל מסך, נשמר ב-state
- **נצנוצים** (`sparkleBurst`) על כל הצלחה, **קונפטי** על סיומים, **רצף קומבו** בארקייד (מ-3 תפיסות רצופות: בונוס מטבעות + צליל)
- **בועת הינשוף נכתבת אות אחרי אות** (לחיצה מדלגת)

## 🛟 גלגל הצלה: ניקוד
כפתור 🛟 (במפה וביחידה) מציג את כל הטקסט עם ניקוד. מבוסס על מילון מילה←מנוקדת
ב-`app/js/nikud.js`. מילה שחסרה במילון מוצגת בלי ניקוד (לא נשבר כלום).
**כל יחידה חדשה חייבת להוסיף למילון את כל המילים של הטקסטים והמשחקונים שלה.**

## 🎬 מהפכת המדיה (בתהליך)
אפיון מלא למעבר לתמונות 4K ווידאו: [media-redesign-spec.md](media-redesign-spec.md) —
מלאי נכסים, פרומפטים ל-GPT ול-Kling, נקודות חיבור בקוד וסדר פאזות.

## ⚠️ כללי כתיבת תוכן ליחידות חדשות
- **לגוון את מיקום המשפט המרכזי** בפסקאות — לא תמיד ראשון! (פידבק מהמשתמשת: זה היה צפוי מדי).
  ביחידה 1 אלף הינשוף כבר מלמד את זה כטיפ ("לפעמים הוא מחכה בסוף")
- תשובות רב ברירה נכתבות בכל סדר — המנוע מערבב לבד

## דמות מלווה חוזרת
**אלף הינשוף** (OWL_SVG ב-data.js) מדבר בבועת דיבור בכל שלב (`part.guide`).
כל יחידה חדשה חייבת לכלול אותו — הוא העוגן הרגשי החוזר של הקורס.

## 👾 מוטיב ויזואלי אחיד: מפלצות מגעילות מצחיקות
החלטת עיצוב (של המשתמשת): **מוטיב אחד לכל המשחקונים — מפלצות**, לא ערבוב סגנונות.
`MONSTER_SVG(color, dark, variant)` עם 3 גרסאות: רגילה / קיקלופ / שלוש עיניים ולשון.
בארקיידים המפלצות נושאות את המילים (עם בלון 🎈 או כנפי עטלף 🦇); במשחק ההאכלה הן אוכלות.
יחידות חדשות ממציאות וריאציות על מפלצות, לא מוטיבים חדשים (דגים/חלליות וכו').

## מערכות המשחק
| מערכת | פירוט |
|--------|--------|
| 🐉 דרקון הקריאה | הדמות היא יצור שמתפתח: ביצת פלא ← בקיע (אחרי יחידה 1) ← דרקון צעיר (3) ← מכונף (5) ← מלכותי (7). `CREATURE_STAGES` + `CREATURE_SVG` ב-data.js. רגע ההתפתחות נחגג במסך הפרס, והשלב הבא מוצג כצללית מסתורית בפרופיל |
| מטבעות | תשובה נכונה בניסיון ראשון +10, שני +5, השלמת יחידה +50 |
| פסילות | 3 פסילות בפעילות = התחלה מחדש של הפעילות (לא של היחידה) |
| פריטי מסע | פריט ייחודי בסוף כל יחידה (מהסילבוס), נכנס לספר המסע |
| חנות | שדרוגי דמות (כובעים, אביזרים, חיות מחמד, צבעים) במטבעות |
| ספר מסע | פריטים שנאספו + "מילה שאקח איתי" מכל יחידה |
| מפה | תחנות נעולות נפתחות בהדרגה; הדמות עומדת בתחנה הנוכחית |

## החלטות טכניות
- **אפליקציה אחת** — `app/index.html` מכיל את כל העולם; יחידות הן data ב-JS, לא קבצים נפרדים
- **שמירת מצב**: localStorage (מפתח `smart-readers-save-v1`). חיבור Supabase/TeachPal — שלב עתידי
- **גרפיקה**: תמונות AI מאוירות (סגנון התמונה `ChatGPT Image Apr 23...png` — עולם טבע מאויר, שלטי עץ).
  עד שהתמונות מוכנות: placeholders ב-SVG/CSS. פרומפטים: [image-prompts.md](image-prompts.md)
- Vanilla JS, ללא Tailwind, Lucide רק ל-UI, גופנים Rubik + Heebo, RTL מלא
- מובייל פירסט — כל אינטראקציית גרירה חייבת גם מצב הקשה (tap-tap)

## מיקרוקופי לגילאי 8–10
- קצר, חם, ישיר. "בואו נבדוק", "כמעט! נסו שוב"
- טעות היא חלק מהמשחק — הפידבק מסביר, לא נוזף
- יעדי למידה בגוף ראשון: "בסוף היחידה אדע:"
- להימנע ממקפים באמצע משפט — לנסח מחדש במקום

## קבצי מקור (docx בתיקייה זו)
- `syllabus-smart-readers.docx` — הסילבוס המקורי (הומר ל-syllabus.md)
- `unit1-smart-readers (1).docx` — מפרט מלא של יחידה 1 (מוטמע ב-app/js/data.js)
- `קורס שיפור הבנת הנקרא שלושה ציר תוכן מרכזיים.docx` — ההיגיון הפדגוגי של הצירים

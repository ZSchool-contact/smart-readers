# קוראים חכמים — סטטוס פרויקט ומדריך המשך

> מסמך העברה לשיחה חדשה. עודכן: 29.7.2026
> קרא קודם את [CLAUDE.md](CLAUDE.md) (עקרונות) ואת המסמך הזה (סטטוס + איך ממשיכים).

## 🔗 קישורים
- **ריפו**: https://github.com/ZSchool-contact/smart-readers (חשבון ZSchool-contact, gh מחובר)
- **המשחק החי**: https://zschool-contact.github.io/smart-readers/ (GitHub Pages, מתעדכן אוטומטית עם כל push)
- מיקום מקומי: `courses/hebrew/` — ריפו git עצמאי (מוחרג מהריפו ההורה ב-.gitignore)

## ✅ מה בנוי ועובד (הכל נבדק E2E)

### העולם
- **מפה** עם 9 תחנות (0–8), נעילה מדורגת, דמות שצועדת בין תחנות. רקע GPT מלא
- **דרקון הקריאה** 🐉 — הדמות היא יצור מתפתח: ביצת פלא ← בקיע (אחרי יחידה 1) ← דרקון צעיר (3) ← מכונף (5) ← מלכותי (7). רגע ההתפתחות נחגג במסך הפרס; השלב הבא מוצג כצללית מסתורית בפרופיל. כרגע SVG — גיליון GPT ממתין ליצירה
- **חנות** — כובעים/משקפיים/חיות/צבעים במטבעות; הפריטים באמת מתלבשים על הדרקון
- **ספר מסע** — פריט ייחודי מכל יחידה + "מילה שאקח איתי"
- **הרשמה** (יחידה 0 לייט) — שם, בחירת ביצת פלא, עולם תוכן אישי
- **שמירה** — localStorage (`smart-readers-save-v1`), פר דפדפן. Supabase = שלב עתידי מוגדר
- **סאונד** — WebAudio מסונתז (sound.js), כפתור השתקה
- **🛟 גלגל הצלה** — ניקוד לכל הטקסט (מילון ב-nikud.js) + אונבורדינג חד־פעמי בכניסה הראשונה ליחידה

### יחידה 1 "בונים עולמות" — שלמה (12 שלבים)
הטרמה בתמונה ← משפט אחד ← ארקייד בלונים ← קטע קצר ← שאלת הבנה ← ארקייד לילה ← פסקה גדולה ← כותרות ← האכלת מפלצות ← טעות בכוונה (מפלצת השגיאות) ← אנקדוטה ← סיכום

### יחידה 2 "בעלי חיים מדהימים" — שלמה (12 שלבים, פורסמה 29.7.2026)
אותו מבנה מדורג כמו יחידה 1. משפחות ר־ו־צ / ז־נ־ב / ח־י־ה, כתיב מילות טבע עם ט׳ (טל, טיול, טבע, טחב).
כל התמונות נוצרו (Codex) וחוברו, כולל תמונה ייעודית לכל קטע קריאה. מילון הניקוד הורחב ב-221 מילים.
קבצים נלווים בשורש הקורס: `unit2-smart-readers.docx` (התוכן ב-Word), `unit2-image-prompts.md`
(תבנית פרומפטים ל-Codex — לשכפל ליחידות הבאות).
**החלטות שנקבעו ביחידה 2 (מחייבות להמשך):**
- רקע המסך מסביב לכרטיס נשאר קבוע לכל הקורס; רק `cardBg` (בתוך הכרטיס) ייעודי פר יחידה
- לכל קטע קריאה תמונה ייעודית משלו — לא לחזור על אותה תמונה בין שלבים
- שגיאות הכתיב המכוונות של "טעות בכוונה" לא נכנסות למילון הניקוד (שלא "נאשר" כתיב שגוי)

### יחידה 3 "שפה שמחברת" — שלמה כולל תמונות (נבנתה 29.7.2026, תמונות חוברו 30.7.2026)
המפגש הייעודי למשפחות מילים (⭐⭐, פריט 🌱 זרע שורש). כפל המשמעות של הכותרת: השורש מחבר
מילים למשפחה, והשפה מחברת אנשים — לכן שלושת השורשים הם שורשי תקשורת: ש־מ־ע / ד־ב־ר / כ־ת־ב,
והטקסטים עוסקים ביונות דואר ובמכתבים בין ילדים לסבים.
**עליית הקושי שמומשה (לפי מפת הפרוגרסיה):** מסיחים "מתחפשים" בארקיידים (שמש, כתר — אותיות
דומות בלי השורש המלא), השלמת משפט עם מילה חסרה, כותרות מ-2 אפשרויות קרובות בלבד, שאלת
הסקה ראשונה ("למה לדעתכם..."), וטעות בכוונה של בן משפחה שגוי (שומע/משמיע) במקום שגיאת כתיב.
**עדכון 30.7 (בקשת שני):** המשחקון השני (שלב 6, כ־ת־ב) הוחלף מארקייד הדואר ל**משחק
המחבואים** (mole-game) — הוא אהוב במיוחד. ערכת `mail` של catch-game נשארה במנוע + CSS +
`arcade-mail.png`, זמינה ליחידות הבאות. תוקן גם fallback של word-hunt שתמיד צייר את סצנת יחידה 1.
מילון הניקוד הורחב (~210 מילים, כיסוי מלא אומת בסקריפט). E2E מלא עבר: כל 12 השלבים + פרס +
פתיחת תחנה 4, אפס שגיאות קונסולה (`tools/e2e_unit.py 3`).
**התמונות חוברו (30.7.2026):** כל 9 התמונות מ-`unit3-image-prompts.md` נוצרו וחוברו, כולל
`arcade-mail.png` (במקום הגרדיאנט) ו-`item-3.png` בספר המסע. גיליון הדמויות
`unit3-characters-ref.png` שמור כרפרנס לעקביות בתמונות עתידיות של דנה, הסבים והיונה.

### חוויית מסך היחידה
- **מסגרת אחת מאוחדת בגודל מצגת**: אלף הינשוף + הנחיות בשליש הימני (קבוע), הפעילות בשני השלישים השמאליים (מתחלף)
- **אפס גלילה**: fit-to-screen — המסגרת מתכווצת אוטומטית (ResizeObserver + transform scale)
- **רקעים**: מסביב = רקע העולם; בתוך המסגרת = רקע כרטיס פר יחידה (`cardBg`)
- **תיבות אוצר**: כל שאלה אמריקאית = תיבות עץ שנפתחות עם זהב. התשובות מתערבבות אוטומטית
- **סקין משחקי**: שלטי עץ, פאנלים קלף, אסימוני זהב, שאלות 🎯, בועת אלף נכתבת באות־אות

## 📁 מפת קבצים
```
app/js/data.js    תוכן היחידות (UNITS), מפה, חנות, דרקון (CREATURE_*), מפלצות (MONSTER_SVG),
                  ינשוף (OWL_SVG), תיבות (CHEST_*), סצנות SVG fallback (SCENE_SVGS), ASSETS
app/js/engine.js  מנוע החלקים: word-hunt / sentence-hunt / catch-game / feed-game / reading /
                  mcq-set / sort-families / anecdote / summary + fitUnitCard + partHeader (פאנל צדדי)
app/js/world.js   מפה, הרשמה, חנות, ספר מסע, preloadAssets, renderAvatar, אונבורדינג גלגל הצלה
app/js/state.js   localStorage, מטבעות, contentUnitsDone
app/js/nikud.js   מילון ניקוד (~330 מילים) + nikkudize/refreshNikud
app/js/sound.js   אפקטים קוליים
app/assets/img/   נכסי GPT (raw/ = מקורות לפני חיתוך)
image-prompts.md  פרומפטים לכל תמונה עתידית + הנחיות סגנון מחייבות
media-redesign-spec.md  אפיון מדיה מלא (כולל וידאו, פאזות 2-3)
syllabus.md       9 היחידות במלואן
```

## 🖼️ סטטוס נכסים ויזואליים
| קיים (GPT) | חסר (פרומפטים מוכנים ב-image-prompts.md) |
|------------|-------------------------------------------|
| רקע מפה, אלף הינשוף, 6 מפלצות, 3 סצנות יחידה 1, רקע כרטיס יחידה 1 | גיליון דרקון (5 שלבים × 5 צבעים), אווטארים/פריטי חנות מאוירים, מדליוני תחנות, רקעי ארקייד, כל הווידאו (פאזה 2) |

יצירת תמונות: בשיחת GPT "Children's Game World Map" בחשבון של שני (לשמור סגנון!). אפשר גם
דרך הדפדפן (Claude in Chrome) — לשים לב למגבלת קצב אחרי ~5 תמונות.

## 🛠️ איך בונים את יחידה 5 (וכל יחידה הבאה)
1. **תוכן**: להוסיף `UNITS[5]` ב-data.js לפי [syllabus.md](syllabus.md) (יחידה 5: אוכל מסביב
   לעולם — ⭐⭐⭐, פריט ⭐ כוכב נסתר, שורשים ב־ש־ל / א־כ־ל / ט־ע־מ, השלמת כותרת חסרה,
   הרחבת משפחה עצמאית). להשתמש באותם part types (כולל mole-game החדש!).
   מבנה מדורג חובה: משפט ← משחקון ← קטע קצר ← משחקון ← פסקה
2. **מפרט מלא של השלבים** נמצא ב-CLAUDE.md (בנק המשחקונים + כללי כתיבה: לגוון מיקום משפט מרכזי,
   תשובות בכל סדר, מוטיב מפלצות בלבד, goal=10 בארקיידים)
3. **ניקוד**: להוסיף ל-nikud.js את כל המילים החדשות (טקסטים, משחקונים, שאלות)
4. **תמונות**: סצנות לפי התבנית (sceneImage + sceneSvg fallback + sceneSize), רקע כרטיס `cardBg`
   ייעודי, q.img לשאלות. לכתוב קובץ פרומפטים לפי התבנית של `unit2-image-prompts.md`
   (כולל גיליון דמויות כרפרנס עקביות) ולמסור ל-Codex
5. **וריאציית ארקייד**: אותו מנוע catch-game — אפשר theme חדש (למשל מפלצות שוחות במים) בקלות:
   themes ב-renderCatchGame + CSS רקע
6. **בדיקה**: `uv run --with playwright python tools/e2e_unit.py <id>` +
   `tools/nikud_coverage.py <id>` + `tools/nikud_mismatch.py` — הכל חייב לעבור נקי
7. **פרסום**: `git add -A && git commit && git push` בתיקיית הקורס — הלינק החי מתעדכן תוך דקות

## 🧰 tools/ — סקריפטי בדיקה קבועים (מריצים עם `uv run --with playwright python`)
- `tools/e2e_unit.py <id>` — עובר יחידה שלמה כמו לומד: כל השלבים, פרס, פתיחת התחנה הבאה, אפס שגיאות
- `tools/nikud_coverage.py <id>` — מוודא שכל מילות היחידה קיימות במילון הניקוד
- `tools/nikud_mismatch.py` — מאתר ערכי ניקוד שמשנים אותיות (כתיב חסר) — חייב להחזיר 0
- `tools/fix_nikud_male.py [apply]` — ממיר אוטומטית ערכים כאלה לכתיב מלא מנוקד

## ⚠️ מלכודות שנלמדו (לא לחזור עליהן)
- **מילון הניקוד חייב כתיב מלא** — ניקוד בכתיב חסר "מעלים" אותיות (דיבור→דִּבּוּר) ומבלבל
  את הילדים (פידבק 30.7). כל 160 הערכים הומרו; בכל יחידה חדשה להריץ `tools/nikud_mismatch.py`
- **תמונות סצנה בקטעי קריאה לא נחתכות** — `.hunt-scene.small` מציג את התמונה שלמה (contain,
  מסגרת שמתכווצת סביבה); היה object-fit: cover שחתך ראשים (פידבק 30.7)
- `const` ב-data.js לא נגיש דרך `window` — סצנות SVG נרשמות ב-`SCENE_SVGS`
- `url()` בתוך CSS custom property נפתר יחסית לגיליון — להמיר לכתובת מלאה עם `new URL(p, document.baseURI)`
- תווי ניקוד: לא למחוק את המקף העברי ־ (U+05BE) — הוא חלק מכתיבת השורשים
- הבועה הקבועה: לעצור טיימר הקלדה קודם לפני התחלת חדש (guideTypeTimer)
- תמונות ChatGPT: ה-URLs חסומים לחילוץ — מורידים דרך כפתור Save של הצפיין
- קונבנציית התמונות: אם קובץ חסר יש fallback מצויר — האפליקציה לעולם לא נשברת

### יחידה 4 "ספורט ומאמץ" — תוכן שלם, ממתינה לתמונות (נבנתה 30.7.2026)
⭐⭐, פריט 🗝️ מפתח מילים, שורשי הספורט: נ־צ־ח / א־מ־ן / ר־ו־צ (ר־ו־צ חוזרת בכוונה מיחידה 2
כחיזוק זיכרון). 13 שלבים: הטרמה של ניחוש ענף ספורט מחידות (mcq, בכוונה בלי תמונות שלא
יסגירו את התשובה) ← משפט עם 3 מילות משפחה ← ארקייד ← קטע "הסוד של האלופה" ← שאלות +
השלמת משפט ← ארקייד נ־צ־ח ← סיפור בן 4 פסקאות "המרוץ הגדול של יובל" ← כותרת לכל פסקה
מ-2 קרובות + שאלת הסקה ← **משחקון חדש: מפלצות המחבואים** ← האכלה ← טעות בכוונה
(מאמן/מתאמן) ← אנקדוטה (אימון ואמונה מאותו שורש) ← סיכום.
**חדש במנוע (בקשת המשתמשת: עוד משחק חווייתי):** `mole-game` — whack-a-mole של מפלצות
שמציצות ממחילות עם מילים ומתחבאות. קומבו, פסילות, goal — כמו catch-game. זמין לכל היחידות,
ומשולב גם ביחידה 3 שלב 6 (שם הוא מוצג לראשונה ללומד; ביחידה 4 אלף מתייחס אליו כמוכר).
מילון הניקוד הורחב ב-251 מילים (כיסוי מלא אומת). E2E מלא עבר: 13 שלבים + פרס + פתיחת
תחנה 5, אפס שגיאות. סקריפטים כלליים לכל יחידה נשמרו ב-`tools/` (ראו למטה).
**חסר רק:** תמונות AI — פרומפטים מוכנים ב-`unit4-image-prompts.md` (עד אז SVG placeholders).

### יחידה 5 "אוכל מסביב לעולם" — שלמה כולל תמונות (נבנתה ופורסמה 30.7.2026)
⭐⭐⭐, פריט ⭐ כוכב נסתר, שורשים ב־ש־ל / א־כ־ל / ט־ע־מ. 12 שלבים סביב איטליה, יפן וישראל:
הטרמת התאמת מאכל למדינה (feed-game עם מפלצות מדינות) ← משפט עם 3 מילות ט־ע־מ ← ארקייד
א־כ־ל (מסיחים ערמומיים: אוכמניות, אכזבה) ← קטע "לחם מסביב לעולם" ← שאלה מפורשת + הסקה ←
מחבואים ב־ש־ל (בצל מתחפש) ← 3 פסקאות מדינות ← **כתיבה חופשית ראשונה בקורס**:
השלמת כותרת חסרה בהקלדה + הרחבת משפחה עצמאית ← טעות בכוונה (בישל/התבשל) ←
אנקדוטה (מילים נודדות: פיצה, סושי) ← סיכום.
**חדש במנוע:** שני סוגי פעילות כתיבה — `cloze-title` (השלמת כותרת, רמז אחרי 2 טעויות)
ו-`family-expand` (אימות שורש אלגוריתמי: אותיות השורש לפי הסדר + נרמול אותיות סופיות,
בלי פסילות — כתיבה לא מענישה). מילון הניקוד הורחב ב-244 מילים.
E2E הורחב לשני הסוגים החדשים ועבר מלא + רגרסיה על 3–4.
**התמונות חוברו (30.7):** כל 7 התמונות מ-`unit5-image-prompts.md` נוצרו וחוברו, כולל
`item-5.png` בספר המסע. רקע הכרטיס העז נראה מצוין עם שכבת ההבהרה של 30%.

## 🔜 צעדים פתוחים (לפי סדר ערך)
1. יחידה 6 "חלל ומסתורין" — הבא בתור (⭐⭐⭐: כותרות מעורבות — 2 קרובות + אחת עצמאית,
   מציאת שורש + כתיבת מילה מאותה משפחה, שורשים ח־ק־ר / כ־ו־כ / ע־ו־פ, פריט 🧭 מצפן)
2. גיליון התפתחות הדרקון ב-GPT (משדרג את הדמות מהותית)
3. יחידה 0 מלאה (כרגע הרשמה לייט מכסה אותה)
4. Supabase לשמירה חוצת מכשירים (~יום עבודה, מדריך PUBLISHING-TO-TEACHPAL.md בשורש)
5. וידאו (פאזה 2 באפיון): אמביינט מפה, אינטרו עולם, חגיגת סיום
6. פריטי חנות מאוירים במקום אימוג'י

<!-- flowpad:capsule identity
version: 1
data:
  id: ca346297-de98-4d0d-bf2e-0cac24fb8d7e
flowpad:endcapsule identity -->
